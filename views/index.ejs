<!DOCTYPE html>
<html ng-app="temperatureApp">
  <head>
    <meta charset="utf-8">
    <title></title>
    <style link="/angular/angular-csp.css"></style>
  </head>
  <body ng-controller="TemperatureCtrl as tempCtrl" ng-cloak>
    <h1><%= title %></h1>
    <div>
      <div class="date">{{ temperature.datetime | date : 'd.MM.yyyy' }}</div>
      <div class="time">{{ temperature.datetime | date : 'HH:mm:ss' }}</div>
      <div class="celsius">{{ temperature.celsius }}</div>
    </div>

    <pre>
      <hr/>
      <% if(env !== undefined) { %>
        env:<%= env %>
      <% } %>
    </pre>
    <script type="text/javascript" src="/angular/angular.js"></script>
    <script type="text/javascript" src="/angular-resource/angular-resource.js"></script>
    <script type="text/javascript" src="javascripts/app.js"></script>
  </body>
</html>
